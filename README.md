# Bride-to-Be Party Invitation

Static mobile invitation page for GitHub Pages.

## GitHub Pages

Upload this folder's contents to the `main` branch. In GitHub, go to
`Settings` -> `Pages` -> `Build and deployment`, then choose `GitHub Actions`.
The included workflow will deploy the static site after every push to `main`.
